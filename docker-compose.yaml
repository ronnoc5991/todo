services:
  server:
    build: ./server
    command: npm run watch
    ports:
      - "3000:3000"
    volumes:
      - ./server:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./server/node_modules
